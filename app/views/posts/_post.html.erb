<div id="<%= dom_id post %>" class="card p-5 col-6">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <% post.images.each do |image| %>
    <%= image_tag(image, style: "width: 300; height: auto") %>
  <% end %>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
